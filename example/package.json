{
	"name": "rust-fel-example",
	"version": "1.0.0",
	"description": "[View documentation for this example online][dox]",
	"main": "index.js",
	"scripts": {
		"start": "tsc --watch",
		"cargo": "sh build.sh",
		"build-ts": "tsc",
		"cypress:open": "cypress open",
		"cypress:run": "cypress run",
		"ci": "npm run cargo && wait-on http://localhost:8001 && npm run cypress::run",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "",
	"license": "ISC",
	"devDependencies": {
		"cypress": "^4.12.1",
		"wait-on": "^5.2.0"
	}
}
